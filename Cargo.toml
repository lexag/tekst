[workspace]
members = ["common", "receiver", "control"]
resolver = "3"

[workspace.package]
version = "0.2.0"
edition = "2024"
repository = "https://github.com/lexag/tekst"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
